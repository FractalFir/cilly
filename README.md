# cilly
A rust toolchain, generating C from Rust, adapting to C compilers
> [!CAUTION]
> This is not the version of `cilly` which compiled the entirety of `rustc` to `C`(see `crust`).
> While working, that version had some... unfortunate decisions made, which made it unsuitable for my goals.
> This is another, from scratch attempt at the project(this time wholey in the open, so my perfectionism can't make me do a "Rewrite ITTTTT" again). 
